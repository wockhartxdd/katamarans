# План API

- GET /catamarans — список катамаранов
- GET /catamarans/{id} — информация о катамаране
- POST /bookings — создать бронь
- GET /bookings/{id} — статус брони
- DELETE /bookings/{id} — отменить бронь